{
  "name": "anchorfall-desktop",
  "version": "1.18.2",
  "description": "MONOLYTHIUM — THE ANCHORFALL — native desktop shell (Electron) around the web game",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prepare:runtime": "node scripts/prepare-runtime.js",
    "dist": "npm run prepare:runtime && electron-builder",
    "dist:dir": "npm run prepare:runtime && electron-builder --dir",
    "dist:mac": "npm run prepare:runtime && electron-builder --mac",
    "dist:win": "npm run prepare:runtime && electron-builder --win",
    "dist:linux": "npm run prepare:runtime && electron-builder --linux"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^26.0.0"
  },
  "dependencies": {
    "@kmamal/sdl": "^0.11.13"
  }
}
