node_modules
saves
desktop
test
docs
# scripts/ holds dev-only map/asset generators + playtests, but build.js is the
# TS transpile step the Dockerfile runs (npm run build) — it MUST be uploaded.
scripts/*
!scripts/build.js
*.md
*.map
.DS_Store
.git*
.claude/
# music streams from the jsDelivr CDN on the public web build (see audio.js) —
# keep it out of the deploy upload so railway up stays fast (~30MB not ~70MB)
public/assets/audio/music
